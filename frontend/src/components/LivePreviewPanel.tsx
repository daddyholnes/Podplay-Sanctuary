import React from 'react';
import './Layout.css'; // Assuming common styles are in Layout.css

const LivePreviewPanel: React.FC = () => {
  return (
    <div className="panel-placeholder live-preview-panel" style={{ backgroundColor: '#202020', flexGrow: 1 }}>
      Live Preview Panel Placeholder
    </div>
  );
};

export default LivePreviewPanel;
